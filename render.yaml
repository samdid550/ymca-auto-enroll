services:
  - type: worker
    name: ymca-auto-enroll
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    autoDeploy: true

cron:
  - name: weekly-enroll
    schedule: "0 20 * * 0"   # Sunday at 8 PM
    command: "python main.py"
